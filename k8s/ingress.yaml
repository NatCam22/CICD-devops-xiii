apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fastapi-cicd
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: fastapi.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fastapi-cicd
                port:
                  number: 80